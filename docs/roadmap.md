# Roadmap

| Phase | Goal | Gate |
|---|---|---|
| 0 Discovery | contracts, ADRs, reality spikes | fresh-context architecture review reconciled |
| 1 Safe kernel + shell | contracts, vault-markdown, domain, stores, worker, PWA shell, CI | fresh-context adversarial review of mutation/identity/retry |
| 2 Disposable E2E | real Git round trips via LocalGitStore + desktop clone | whole-system midpoint review |
| 3 Live canary prep | GitHub App, Access, deploy, real worker verified | **human approval before first live write** |
| 4 First daily-use release | Today, All, complete, Undo, Done today, captures, states, queue | real iPhone acceptance + whole-system QA |
| 5 Bounded extensions | one at a time | per-increment review |

Phase 5 candidates (not committed): journal capture/check-ins, Waiting/Someday representation,
Focus references (needs `🆔`), scheduling edits, Upcoming, Areas/Projects, Quick Find (may need
`vault-index`), Needs You, recurrence completion, calendar, push, scoped AI.

Deferred-infrastructure triggers: D1 — `docs/indexing.md`; Git Data API — first multi-file atomic
command; Durable Objects — measured write contention or multi-device offline mutation queue.
