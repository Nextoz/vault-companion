import type { ParseResult } from './api.ts';

/** Parse `Tasks/To-Do List.md` per docs/vault-contract.md §2. */
export function parseTodoList(_text: string): ParseResult {
  throw new Error('not implemented');
}
